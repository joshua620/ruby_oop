puts "The error is generated because the Person class only has a getter method, not a setter method."
puts "You could change the attr_reader to an attr_accesor to allow for objects of the Person class"
puts "to have their names changed."